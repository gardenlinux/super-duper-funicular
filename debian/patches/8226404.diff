# DP: https://bugs.openjdk.java.net/browse/JDK-8226404

--- a/make/autoconf/bootcycle-spec.gmk.in
+++ b/make/autoconf/bootcycle-spec.gmk.in
@@ -62,3 +62,6 @@
 endif
 # The bootcycle JVM arguments may differ from the original boot jdk.
 JAVA_FLAGS_BIG := @BOOTCYCLE_JVM_ARGS_BIG@
+
+# The bootcycle JVM cannot use the configure-support/classes.jsa generated by the original boot jdk.
+JAVA_FLAGS += -Xshare:off
